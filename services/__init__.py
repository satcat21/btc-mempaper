"""MempaperApp method groups, split out as mixins.

Each module owns one area of the device's behaviour. MempaperApp inherits
them all, so every method keeps the same `self` and the same call sites.
"""
