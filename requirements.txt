# Pinned so every device resolves the same wheels; the web updater runs
# `pip install -r requirements.txt`. Bumping: docs/MAINTENANCE_GUIDE.md.

pip==26.2.1  # wheel selection on armv6l changes between pip releases

flask==3.1.3
flask-socketio==5.6.1
flask-compress==1.25
brotli==1.1.0  # 1.2.0's armv6l wheel is listed on piwheels but refused (403); retry later
pillow==12.3.0  # Pi Zero (ARMv6): build from source, prebuilt wheels crash with SIGILL
qrcode[pil]==8.2
babel==2.18.0
requests==2.34.2
urllib3==2.7.0  # transitive via requests; pinned so updates actually move it
python-dotenv==1.2.3
websocket-client==1.9.2
werkzeug==3.1.8
watchdog==6.0.0
argon2-cffi==25.1.0
schedule==1.2.2
# Held: piwheels builds newer releases for ARMv8 under armv6l/armv7l names,
# forcing a long source build on 32-bit Pis. Before raising, run
# `python -m utils.wheel_platform` on an ARMv6 device.
numpy==2.2.5
pilmoji==2.0.5

gunicorn==26.2.0
gevent==26.8.0
gevent-websocket==0.10.1

cryptography==50.0.1
psutil==6.0.0

PySocks==1.7.1  # SOCKS5 for requests (Tor)
python-socks==3.1.1  # SOCKS5 for websocket-client, which does not use PySocks

# Asset minifiers for tools/minify.py
rjsmin==1.2.5
rcssmin==1.2.2
